/**
 * stores/index.js
 * 占位文件 — 为未来接入 Pinia 预留位置。
 *
 * 使用说明（未来）：
 * 1. 安装 pinia：`npm install pinia`
 * 2. 在 `src/main.js` 中：
 *    import { createPinia } from 'pinia'
 *    app.use(createPinia())
 *
 * 当前不在项目中引入 pinia，仅导出占位值。
 */

export const storesReady = false
